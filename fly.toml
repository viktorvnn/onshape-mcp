# Fly.io configuration for onshape-mcp HTTP transport.
#
# Deployment:
#   1. fly launch            (creates app, picks region)
#   2. fly certs add onshape.mcp.fstab.net
#   3. DNS: onshape.mcp.fstab.net CNAME onshape-mcp.fly.dev
#   4. fly secrets set \
#        ONSHAPE_MCP_HTTP__ONSHAPE_CLIENT_ID=<your-client-id> \
#        ONSHAPE_MCP_HTTP__ONSHAPE_CLIENT_SECRET=<your-client-secret> \
#   5. Update Onshape OAuth app redirect URI:
#        https://onshape.mcp.fstab.net/oauth/callback
#   6. fly deploy
#   7. Add as Claude.ai connector:
#        https://onshape.mcp.fstab.net/mcp

app = "onshape-mcp"
primary_region = "iad"
# Custom domain: onshape.mcp.fstab.net
# Setup: fly certs add onshape.mcp.fstab.net
# DNS:   onshape.mcp.fstab.net CNAME onshape-mcp.fly.dev

[build]

[env]
# Listen on all interfaces so Fly's proxy can reach the app.
ONSHAPE_MCP_HTTP__HOST = "0.0.0.0"
ONSHAPE_MCP_HTTP__PUBLIC_URL = "https://onshape.mcp.fstab.net"
# Secrets (set via `fly secrets set` or the Fly.io dashboard):
#   ONSHAPE_MCP_HTTP__ONSHAPE_CLIENT_ID
#   ONSHAPE_MCP_HTTP__ONSHAPE_CLIENT_SECRET

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = "stop"
auto_start_machines = true
min_machines_running = 0

[[http_service.checks]]
path = "/health"
interval = "30s"
timeout = "5s"
